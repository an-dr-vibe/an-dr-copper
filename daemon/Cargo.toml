[package]
name = "copperd"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
default-run = "copper"

[lib]
name = "copperd"
path = "src/lib.rs"

[[bin]]
name = "copper"
path = "src/main.rs"

[dependencies]
bones-bus = { package = "bus", path = "../bones/core/bus" }
bones-logging = { package = "logging", path = "../bones/core/logging" }
bones-messages = { path = "../bones/shared/bones-messages" }
bones-runner = { package = "runner", path = "../bones/core/runner", default-features = false }
bones-web = { package = "web", path = "../bones/core/web", default-features = false, features = ["wry-backend"], optional = true }
clap = { version = "4.5.40", features = ["derive"] }
ctrlc = "3.4.7"
dirs = "6.0.0"
jsonschema = "0.18.3"
keyring = { version = "3", features = ["windows-native", "apple-native", "linux-native"] }
rand = "0.8.5"
resvg = "0.45.1"
semver = "1.0.25"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
thiserror = "2.0.12"
tiny_http = "0.12.0"
ureq = { version = "2.12.1", features = ["json"] }
walkdir = "2.5.0"

[features]
default = ["native-ui"]
native-ui = ["dep:bones-web"]

[dev-dependencies]
assert_cmd = "2.0.16"
predicates = "3.1.3"
tempfile = "3.20.0"

[target.'cfg(windows)'.dependencies]
tray-item = "0.10.0"
windows-sys = { version = "0.61.2", features = ["Win32_Foundation", "Win32_UI_Input_KeyboardAndMouse", "Win32_UI_WindowsAndMessaging", "Win32_UI_Shell", "Win32_System_LibraryLoader", "Win32_Graphics_Gdi", "Win32_Storage_FileSystem"] }

[target.'cfg(target_os = "macos")'.dependencies]
tray-item = "0.10.0"

[target.'cfg(target_os = "linux")'.dependencies]
tray-item = { version = "0.10.0", default-features = false, features = ["ksni"] }
