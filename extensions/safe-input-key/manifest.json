{
  "$schema": "https://Copper.dev/schemas/extension/1.0.0/descriptor.schema.json",
  "id": "safe-input-key",
  "name": "Safe Input Key",
  "version": "1.0.0",
  "trigger": "safe-input-key",
  "permissions": ["keyboard", "secure-store", "store", "ui"],
  "inputs": [
    {
      "id": "triggerKey",
      "type": "hotkey",
      "label": "Trigger key / combo",
      "description": "Key or combo registered by the Copper daemon on Windows (e.g. scroll_lock, ctrl+alt+f12).",
      "default": "scroll_lock"
    },
    {
      "id": "text",
      "type": "text",
      "label": "Text to save",
      "description": "Text saved in Copper settings and copied to the OS keychain when you run Save text.",
      "default": ""
    }
  ],
  "actions": [
    {
      "id": "type-text",
      "label": "Type saved text",
      "description": "Type the securely stored text at the current cursor position.",
      "script": "typeText"
    },
    {
      "id": "setup",
      "label": "Save text",
      "description": "Store the text to type in the OS keychain. Pass the value with --input text=<value>.",
      "script": "setup"
    },
    {
      "id": "clear",
      "label": "Clear saved text",
      "description": "Delete the stored text from the OS keychain.",
      "script": "clearText"
    }
  ],
  "settings": {
    "title": "Safe Input Key",
    "description": "Press a key to securely type a stored text (password, snippet, one-time phrase).",
    "applyActions": [],
    "tabs": [
      {
        "id": "config",
        "title": "Configuration",
        "sections": ["trigger-key", "saved-text"],
        "showCommands": true
      }
    ],
    "sections": [
      {
        "id": "trigger-key",
        "title": "Trigger key",
        "description": "The key or combo the Copper daemon registers for Safe Input Key on Windows.",
        "inputs": ["triggerKey"]
      },
      {
        "id": "saved-text",
        "title": "Saved text",
        "description": "Save settings first, then run Save text to copy this value into the OS keychain. Run Clear saved text to delete the keychain entry.",
        "inputs": ["text"]
      }
    ]
  },
  "ui": {
    "type": "form"
  }
}
